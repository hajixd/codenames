// Rename this file to config.js and fill in your Firebase web app config.
// You can find this in Firebase Console -> Project settings -> Your apps (Web) -> Firebase SDK snippet (Config).
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID",
  // Optional (recommended for Firestore + Auth):
  // measurementId: "G-XXXXXXX",
};
