# Black Tabs Site

A minimal black website with three centered tabs:
- Teams
- Bracket
- Rules

## Edit
- Source TypeScript lives in `src/app.ts`.
- The browser loads `app.js`.

## Build (optional)
From this folder:

```bash
npm i
npm run build
```

Open `index.html`.
